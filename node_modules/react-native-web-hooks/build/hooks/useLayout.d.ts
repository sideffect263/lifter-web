import { LayoutChangeEvent } from 'react-native';
declare type Layout = {
    width: number;
    height: number;
    x: number;
    y: number;
};
declare type OnLayout = (event: LayoutChangeEvent) => void;
export default function useLayout(): Layout & {
    onLayout: OnLayout;
};
export {};
