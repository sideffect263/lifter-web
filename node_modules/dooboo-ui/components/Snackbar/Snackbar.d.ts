import React from 'react';
import type { StyleProp, TextStyle, ViewStyle } from 'react-native';
import type { ReactElement } from 'react';
import type { SnackbarType } from '../Styled/StyledComponents';
type Styles = {
    container?: StyleProp<ViewStyle>;
    text?: StyleProp<TextStyle>;
    type?: SnackbarType;
};
export interface SnackbarProps {
    testID?: string;
    ref: React.MutableRefObject<SnackbarRef>;
}
export type SnackbarContent = {
    timer?: SnackbarTimer;
    styles?: Styles;
    actionText?: string;
    onActionPress?: () => void;
    type?: SnackbarType;
    content: {
        element?: ReactElement;
        text?: string;
    };
};
export interface SnackbarRef {
    show(content: SnackbarContent): void;
}
export declare enum SnackbarTimer {
    SHORT = 1500,
    LONG = 3000
}
export declare const Snackbar: React.ForwardRefExoticComponent<Pick<SnackbarProps, "testID"> & React.RefAttributes<SnackbarRef>>;
export {};
