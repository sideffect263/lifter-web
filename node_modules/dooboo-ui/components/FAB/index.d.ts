import type { StyleProp, ViewStyle } from 'react-native';
import type { ButtonSizeType } from '../Button';
import type { IconName } from '../Icon';
import type { ReactElement } from 'react';
interface Styles {
    FAB?: StyleProp<ViewStyle>;
    FABItem?: StyleProp<ViewStyle>;
    buttonSize: ButtonSizeType;
    iconSize?: number;
    gap?: number;
}
export interface FABItem {
    icon: IconName;
    id: string;
}
export interface FABProps<Item extends FABItem> {
    isActive: boolean;
    FABItems: Item[];
    onPressFAB: () => void;
    onPressFABItem: (item?: Item) => void;
    renderFAB?: () => ReactElement;
    renderFABItem?: (item: Item, idx: number) => ReactElement;
    style?: StyleProp<ViewStyle>;
    styles?: Styles;
}
declare function FloatingActionButtons<Item extends FABItem = FABItem>({ isActive, style, styles, FABItems, onPressFAB, onPressFABItem, renderFAB, renderFABItem, }: FABProps<Item>): ReactElement;
export { FloatingActionButtons as FAB };
