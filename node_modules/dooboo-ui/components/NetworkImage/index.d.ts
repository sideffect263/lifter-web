import type { ImageProps, ImageRequireSource, StyleProp, ViewStyle } from 'react-native';
import type { ReactElement } from 'react';
interface Props {
    style?: StyleProp<ViewStyle>;
    url: string | undefined;
    loadingSource?: ImageRequireSource | ReactElement;
    imageProps?: Partial<ImageProps>;
    shouldFixImageRatio?: boolean;
}
declare function NetworkImage(props: Props): ReactElement;
export { NetworkImage };
