import React from 'react';
import type { TextStyle, ViewStyle } from 'react-native';
type ProgressType = 'success' | 'danger' | 'warning' | 'info';
interface Props {
    styles?: {
        container?: ViewStyle;
        text?: TextStyle;
        circle?: {
            radius?: number;
            color?: string;
            strokeWidth?: number;
        };
    };
    style?: ViewStyle;
    type?: ProgressType;
    progress: number;
}
export declare const ProgressCircle: React.FC<Pick<Props, keyof Props> & {
    theme?: import("@emotion/react").Theme;
}>;
export default ProgressCircle;
