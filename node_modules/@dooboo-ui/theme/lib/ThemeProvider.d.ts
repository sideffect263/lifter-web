import '@expo/match-media';
import type { Colors, DoobooTheme, ThemeParam, ThemeType } from './colors';
import type { Theme } from '@emotion/react';
import { withTheme } from '@emotion/react';
import type { ColorSchemeName } from 'react-native';
import type { ReactElement } from 'react';
export type ThemeContext = {
    themeType: ColorSchemeName;
    media: {
        isPortrait: boolean;
        isMobile: boolean;
        isTablet: boolean;
        isDesktop: boolean;
    };
    theme: Theme & DoobooTheme;
    changeThemeType: (themeType?: ColorSchemeName) => void;
    colors: Colors;
};
declare const useCtx: () => ThemeContext;
export type ThemeProps = {
    children?: ReactElement;
    initialThemeType?: ThemeType;
    customTheme?: ThemeParam;
};
export declare function ThemeProvider({ children, initialThemeType, customTheme, }: ThemeProps): ReactElement;
export { useCtx as useTheme, withTheme };
